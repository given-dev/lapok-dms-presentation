# LAPOK DMS — Environment configuration
# Copy to .env and adjust for your machine

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=lapok_dms
DB_USER=root
DB_PASS=

# CORS — set to your frontend origin in production
CORS_ORIGIN=*
